#!/usr/bin/env bash

python manage.py migrate

exec uwsgi --chdir=/app \
    --module=project.wsgi:application \
    --env \
    --master \
    --protocol=uwsgi \
    --socket=0.0.0.0:8000 \
    --enable-threads 
    --processes=2
